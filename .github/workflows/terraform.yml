name: Terraform AWS (disabled)
on: []
jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps: []
# Temporarily disabled. Remove the leading '# ' from each line to re-enable this workflow.
# name: Terraform AWS
#
# on:
#   pull_request:
#   push:
#     branches: [main]
#   workflow_dispatch:
#     inputs:
#       terraform_action:
#         description: Terraform operation to run
#         required: true
#         type: choice
#         options:
#           - apply
#           - destroy
#         default: apply
#
# permissions:
#   contents: read
#
# env:
#   TF_IN_AUTOMATION: true
#   AWS_REGION: us-east-1
#
# jobs:
#   terraform:
#     name: Terraform ${{ github.event_name == 'workflow_dispatch' && inputs.terraform_action || github.event_name == 'pull_request' && 'plan' || 'apply' }}
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout
#         uses: actions/checkout@v4
#
#       - name: Setup Terraform
#         uses: hashicorp/setup-terraform@v3
#         with:
#           terraform_version: 1.9.8
#
#       - name: Configure AWS credentials
#         uses: aws-actions/configure-aws-credentials@v4
#         with:
#           aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
#           aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY_ID }}
#           aws-region: ${{ env.AWS_REGION }}
#
#       - name: Terraform init
#         run: terraform init -input=false
#
#       - name: Terraform format
#         run: terraform fmt -check -recursive
#
#       - name: Terraform validate
#         run: terraform validate
#
#       - name: Terraform plan
#         if: github.event_name == 'pull_request' || github.event_name == 'workflow_dispatch' || (github.event_name == 'push' && github.ref != 'refs/heads/main')
#         run: terraform plan -input=false
#
#       - name: Terraform apply
#         if: (github.event_name == 'push' && github.ref == 'refs/heads/main') || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main' && inputs.terraform_action == 'apply')
#         run: terraform apply -input=false -auto-approve
#
#       - name: Terraform destroy
#         if: github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main' && inputs.terraform_action == 'destroy'
#         run: terraform destroy -input=false -auto-approve
